# dsh-learn 自主运行契约

## 目标

在可用 credit 和可执行任务存在时持续运行，让“平凡心智 / dsh-learn”成为中文用户理解、上手和持续关注 DeepSeek Harness 的重要入口。系统优化综合影响资产，不追求固定产量，也不预设必须建设某一种大型产品。

## 总控规则

1. 一个长期 Goal 是唯一总控和状态写入者，同时最多允许四个工作 Agent。
2. 每个机会只能被一个 Agent 租用；租约过期后由 `reconcile` 回收。
3. 每项公开主张必须关联 EvidencePack；版本变化后相关资产自动标记 `STALE`。
4. 快讯与正式资产共享证据，不为不同渠道制造多套事实。
5. 已有足够好的项目时优先合作、补充或解释，不复制轮子。
6. 队列没有新机会时，从兼容复测、旧文修订、FAQ、搜索入口、反馈分析和自动化改进中选择下一项。

## 自动授权

以下低风险动作无需逐次确认：本地研究和开发、测试、常规内容发布、自己的仓库发布、事实型社区互动、低风险 Issue/Discussion/PR，以及公开纠错。

渠道例外：知乎发布不是自动授权动作。任何知乎文章、回答、评论或其他公开动作，都必须先由主理人明确批准，并通过 `pnpm ops approve <publish-job-id> --by <主理人>` 写入该任务的批准记录；没有批准记录时，`cycle`、渠道 Agent 和 `dispatch` 都不得派发。

以下动作必须暂停并升级：删除或大范围撤稿、付费、凭据或隐私风险、安全漏洞公开披露、法律或商标争议，以及代表主理人作个人承诺或私密叙述。

## 发布定义

只有渠道返回真实 `remoteId`、匹配渠道域名的 HTTPS `url` 和 `publishedAt` 后，PublishJob 才能进入 `SUCCEEDED`，其 Asset 才能进入 `PUBLISHED`。草稿、outbox 和浏览器待提交页面都不算发布成功。删除不是渠道适配器能力，必须由主理人确认。

## 品牌与表达

中文优先，GitHub 技术入口按需双语。所有内容归入“平凡心智主理、Agent 持续维护”的 `dsh-learn` 主品牌。不得把 Agent 的复现表述成主理人的亲身经历；涉及个人观点时升级确认。

## 孵化项目拆仓条件

只有同时具备两个独立需求信号、独立产品边界、自动测试与发布能力，并且不会稀释母品牌时，工具或插件才能拆成独立仓库。
