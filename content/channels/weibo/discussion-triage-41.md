官方 Discussions 里，Windows 用户先后报了两类目录问题：一个是 Windows 11 + Firefox 153 中，“添加工作区”的原生选择框跑到浏览器后面；另一个是 rc.6 的目录选择器报 `koffi` 模块找不到。另有用户提到会话归档后没有查看和恢复入口。

这些都还只是用户报告，不能直接写成“Firefox 不兼容”或“重新装 koffi 就能解决”。真要提问，先把 DSH、Node、系统、浏览器、安装方式和最短复现步骤写清楚，路径和私有日志脱敏。对早期项目来说，一条完整的复现记录，比一句“打不开”更有用。
